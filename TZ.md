# техническое задание
### Assignment 1 (HTML) + Assignment 2 (CSS) + задел под будущий движок оценки (JS/backend)
### Тема: online-магазин / байер-сервис **ayana.buyer**

Команда: 3 человека. Центральное правило: **после Assignment 1 новые .html файлы не создаются** — вся карта страниц фиксируется в этом документе один раз, дальше только наполняется и красится.

---

## 0. Правила, действующие на КАЖДОЙ из 8 страниц

- `<!DOCTYPE html>`, `lang="en"`, `charset`, `viewport`, `<meta name="description">`, `<meta name="author" content="Имя">`, уникальный `<title>`.
- Ровно один `<h1>`, без пропуска уровней заголовков.
- Скелет: `<header>` → `<nav>` со ссылками на все 8 страниц → `<main>` → `<footer>` (контакты, `&copy;`).
- HTML-комментарий с именем автора + минимум 2 комментария «почему» (не «что»).
- Никакого `style=`, `<style>`, ссылок на CSS, `<script>` на этом этапе.
- как минимум один `div` и `span` — только там, где реально не подходит ни один семантический тег, и рядом обязательно комментарий, почему. Каждый "неправильный" div стоит 5 штрафных баллов.
- Никакого Lorem ipsum, никаких стоковых или AI-сгенерированных изображений, никаких выдуманных цифр — всё должно опираться на реальные данные аккаунта ayana.buyer (347 постов, 5 997 подписчиков, страны: США/Корея/Турция/Италия/Германия, Kaspi рассрочка, WhatsApp-чат совместных закупок) или на реально собранную информацию (интервью, фото, цитаты).

### 0.1 Открытые вопросы к преподавателю (снять до сдачи)

- Форма одна и та же вставляется на дно каждой из 5 товарных страниц (Обувь, Одежда, 
  Аксессуары, Техника, Посуда) — идентичный HTML-блок копируется вручную. Формально это 
  5 тегов `<form>`, а не один. Если преподаватель имел в виду "один уникальный дизайн формы, 
  можно копировать" — ОК. Если имел в виду "один `<form>` на весь сайт физически" — нужно 
  вместо копирования делать ссылку-якорь на форму, которая физически лежит один раз 
  (например, на index.html), и это стоит уточнить ДО вёрстки, а не после.
- В новой карте нет отдельной "колофон"-страницы — куда девать `code`/`pre`/`kbd`/`samp`? 
  Ниже они распределены по обычным страницам (не идеально естественно), это тоже можно 
  обсудить с преподавателем.

---

## 1. Карта сайта (обновлённая)

| # | Файл | Автор | Назначение |
|---|------|-------|------------|
| 1 | `index.html` | A | Главная: идея, для кого сайт, призыв к действию |
| 2 | `stores.html` | A | Популярные магазины — хаб по всем категориям (как раздел "Популярные магазины" у a2zbuyer) |
| 3 | `reviews.html` | A | Отзывы клиентов + контакты для связи |
| 4 | `shoes.html` | B | Обувь — популярные магазины/бренды обуви + форма заказа |
| 5 | `clothing.html` | B | Одежда — популярные магазины/бренды одежды + форма заказа |
| 6 | `accessories.html` | B | Аксессуары — популярные магазины/бренды + форма заказа |
| 7 | `electronics.html` | C | Техника — популярные магазины/бренды техники + форма заказа |
| 8 | `cookware.html` | C | Посуда — популярные магазины/бренды посуды + форма заказа |

**Общий `<nav>`:** Главная · Популярные магазины · Обувь · Одежда · Аксессуары · Техника · Посуда · Отзывы

---

## 2. Требования по каждому файлу

### index.html — автор A
**Контент (150–250 слов):** кто такие ayana.buyer, чем занимаются (доставка мировых брендов, Kaspi рассрочка, совместные закупки через WhatsApp-чат), для кого сайт, призыв перейти в "Популярные магазины".

**Обязательные элементы:**
- `section` — "Как это работает" (3 шага: выбрать товар → оформить заказ → получить с рассрочкой Kaspi).
- `article` — короткая новость/анонс.
- `figure`+`figcaption` — 1-е из 3 изображений сайта.
- Вложенный список — "Категории товаров": одежда → (обувь, аксессуары), техника → (гаджеты, посуда).
- `strong`/`em` для акцентов.
- Минимум 1 HTML-сущность (из 4 нужных по сайту).
- Комментарий автора + 2 комментария "почему".

### stores.html — автор A
**Контент:** обзорный хаб — какие магазины в целом привозит сервис, история, реальные цифры соцсетей (347 постов, 5 997 подписчиков), география (5 стран).

**Обязательные элементы:**
- **Таблица** с `caption`/`thead`/`tbody`/`th scope="col"` — "Магазин | Категория | Ориентировочный срок доставки".
- `dl`/`dt`/`dd` — термины: "Байер", "Kaspi рассрочка", "Совместная закупка".
- `blockquote`+`cite` — настоящая собранная цитата.
- 2 × `abbr title="…"` — например США, Корея.
- `aside` — врезка-факт ("5 997 подписчиков в Instagram на сентябрь 2026").
- `small` — приписка/дисклеймер под цифрами.

### reviews.html — автор A
**Контент:** отзывы реальных клиентов + контакты (Instagram, WhatsApp, телефон, часы работы) — сюда переехали функции старых about_us/contacts.

**Обязательные элементы:**
- Внешняя ссылка `target="_blank" rel="noopener"` — Instagram/WhatsApp.
- `mailto:` ссылка.
- `tel:` ссылка — реальный формат номера.
- 2 ссылки-якоря на `id` внутри страницы (например, мини-навигация "Отзывы" / "Как связаться").
- `figure`+`figcaption` — 2-е из 3 изображений сайта (например, фото визитки/пункта выдачи).
- `ol start="…"` или `type="I"` — "Порядок оформления заказа".
- `q` — короткая инлайн-цитата от команды поддержки.
- **Комментарий**: если форма всё же физически будет одна на весь сайт (см. п. 0.1), логичное место для неё — здесь, рядом с контактами.

### shoes.html / clothing.html / accessories.html — автор B
**Контент:** для каждой — список популярных магазинов этой категории с описанием (в духе a2zbuyer: Adidas, New Balance, Vans и т.д.), реальные позиции из каталога ayana.buyer.

**Обязательные элементы (распределить между тремя страницами, не дублировать без надобности):**
- **Таблица** с `caption`/`thead`/`tbody`/`th scope` — "Товар | Бренд | Цена | Наличие" (на одной из трёх).
- `ol start="1"` или `reversed` — "Топ-5 популярных позиций" (на другой).
- 3-е из обязательных изображений сайта — фото товара с осмысленным `alt`.
- `mark` — "Скидка"/"Последний размер".
- `sup`/`sub` — сноска у цены.
- Вложенный список — "Категории аксессуаров": сумки → (Jw Pei, Coach), часы → (мужские, женские) — на accessories.html.
- `strong` — бренд, `em` — статус наличия.
- **Форма заказа** (см. риск в 0.1) — внизу каждой из трёх страниц: `method`, `action`, `fieldset`+`legend`, `label`+`id`, `text`(получатель), `tel`, `number`(количество), `date`(дата доставки), `radio`(способ оплаты), `checkbox`(согласие), `select`(способ доставки), `textarea`(комментарий), `required`/`placeholder`, `submit`+`reset`, комментарий "сервера пока нет". Комментарий в коде: "форма идентична на всех товарных страницах — единая форма команды по разрешению преподавателя".

### electronics.html / cookware.html — автор C
**Контент:** аналогично B — популярные магазины по технике (Apple, Sony, JBL) и посуде.

**Обязательные элементы:**
- `blockquote`+`cite` — реальный отзыв о заказе техники/посуды (если не набралось у A).
- `dl` — термины при заказе ("Оригинал", "Гарантия", "Трек-номер") — если ещё не закрыт список.
- `code`/`samp` — пример "промокода" (например, `<code>AYANA10</code>` и вывод статуса заказа через `samp`).
- `hr` — разделитель между списком товаров и формой.
- `br` — там, где уместно разбить строку адреса.
- Ещё 2 × `abbr title="…"`, если не набралось у A.
- Та же **форма заказа**, что и на B-страницах — внизу каждой из двух страниц, с тем же комментарием про единую форму.
- `kbd` — можно органично не влезает без колофона; если преподаватель не против — вставить как второстепенный пример в `aside` ("для быстрого поиска нажмите `<kbd>Ctrl+F</kbd>`"), иначе вынести в открытые вопросы как необязательный.

---

## 3. Чек-лист перед сдачей (обновлён под новую карту)

- [ ] `section`, `article`, `aside`, `figure`+`figcaption` — у каждого автора хотя бы раз.
- [ ] 1 таблица с `caption/thead/tbody/th scope` — у каждого автора.
- [ ] Вложенный список, `ol` с атрибутом, `dl` — закрыты минимум дважды в сумме по сайту.
- [ ] Внешняя ссылка `target+rel`, `mailto`, `tel`, 2 ссылки на `id` — у автора A (на reviews.html).
- [ ] 3 изображения с `alt` на весь сайт.
- [ ] `strong, em, mark, small, sub/sup`, 2×`abbr[title]`, `blockquote`+цитата, `q`, `cite`, `code/samp` (без `pre`/`kbd`, т.к. нет колофона — уточнить у преподавателя), `hr`, `br`, 4 разные HTML-сущности — распределены по сайту.
- [ ] **Форма заказа** — физически один и тот же HTML-код на 5 товарных страницах (B: 3 страницы, C: 2 страницы) — **статус: под вопросом, см. п. 0.1, подтвердить у преподавателя до вёрстки**.
- [ ] Каждый `div`/`span` подписан комментарием "почему не подошёл семантический тег".